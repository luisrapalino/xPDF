[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA: todas las rutas sirven index.html (útil para PWA y recargas directas)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
